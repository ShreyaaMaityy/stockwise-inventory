# React (Vite) Frontend Environment Settings
# Rename to '.env' inside the frontend/ folder

# Public API base gateway url targeting the FastAPI backend.
# Variables must be prefixed with 'VITE_' to be bundled by Vite at compile time.
VITE_API_URL=http://localhost:8000/api/v1
